import { ReportsPageClient } from '@/features/reports/reports-page-client';

export const metadata = {
  title: 'Rapports — Tracking Vehicles',
};

export default function ReportsPage() {
  return <ReportsPageClient />;
}
