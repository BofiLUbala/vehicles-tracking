import { AuditPageClient } from '@/features/audit/audit-page-client';

export const metadata = {
  title: 'Journal d’audit — Tracking Vehicles',
};

export default function AuditPage() {
  return <AuditPageClient />;
}