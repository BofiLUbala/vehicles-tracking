import { SettingsPageClient } from '@/features/settings/settings-page-client';

export const metadata = {
  title: 'Paramètres — Tracking Vehicles',
};

export default function SettingsPage() {
  return <SettingsPageClient />;
}
