import { MissionsPageClient } from '@/features/missions/missions-page-client';

export const metadata = {
  title: 'Missions — Tracking Vehicles',
};

export default function MissionsPage() {
  return <MissionsPageClient />;
}
