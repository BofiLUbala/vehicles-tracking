import { DriversPageClient } from '@/features/drivers/drivers-page-client';

export const metadata = {
  title: 'Chauffeurs — Tracking Vehicles',
};

export default function DriversPage() {
  return <DriversPageClient />;
}
