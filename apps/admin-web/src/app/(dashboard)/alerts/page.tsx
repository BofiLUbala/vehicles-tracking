import { AlertsPageClient } from '@/features/alerts/alerts-page-client';

export const metadata = {
  title: 'Alertes — Tracking Vehicles',
};

export default function AlertsPage() {
  return <AlertsPageClient />;
}
