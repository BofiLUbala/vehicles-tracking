import { UsersPageClient } from '@/features/users/users-page-client';

export const metadata = {
  title: 'Utilisateurs — Tracking Vehicles',
};

export default function UsersPage() {
  return <UsersPageClient />;
}
