import { FuelPageClient } from '@/features/fuel/fuel-page-client';

export const metadata = {
  title: 'Carburant — Tracking Vehicles',
};

export default function FuelPage() {
  return <FuelPageClient />;
}
